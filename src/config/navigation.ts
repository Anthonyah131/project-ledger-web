// config/navigation.ts
// Navigation configuration — defines sidebar menu items and routes
// Each item: { label, href, icon, badge?, children? }
// Supports nested menus and role-based visibility

"use client";

// components/shared/error-boundary.tsx
// Reusable error boundary component — catches render errors in children

export function ErrorBoundary({ children }: { children: React.ReactNode }) {
  return children;
}

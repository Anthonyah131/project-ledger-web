"use client";

// components/providers/query-provider.tsx
// Query provider — wraps app with TanStack Query (React Query) provider
// Manages server state caching, refetching, and mutations

export function QueryProvider({ children }: { children: React.ReactNode }) {
  return children;
}

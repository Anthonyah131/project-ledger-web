// features/prompts/components/prompt-form.tsx
// Prompt submission form — textarea, parameters, submit button
// Sends prompt data to the backend API via services/api

export function PromptForm() {
  return null;
}
